const HomeBtn = ({}) => {
    const goToHome = () => {
        console.log();
    };

    return (
        <button className="main-cont" style={____} onClick={goToHome}>
            Go to Home
        </button>
    );
};

export default HomeBtn;
